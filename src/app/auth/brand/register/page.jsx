import RegisterForm from "@/components/auth/RegisterFormBrand";

export const metadata = {
  title: "Create Account | CollabXSphere",
  description:
    "Register as a Brand or Influencer on CollabXSphere — the AI-powered collaboration platform.",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
